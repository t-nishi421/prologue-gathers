-# .top
-#   = form_with(url: search_books_path, local: true, method: :get) do |f|
-#     .top__items
-#       = f.text_field :keyword, placeholder: '本のタイトルを検索', class: 'top__items__search'
-#       = f.submit "検索", class: "top__items__search-btn"
= render partial: 'main'